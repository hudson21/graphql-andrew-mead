# SET_NULL (default behavior is @relation is not defined) 
# CASCADE --> it is going to delete the data which are involved in the relationship types

type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
}